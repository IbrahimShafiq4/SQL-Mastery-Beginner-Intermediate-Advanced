# SQL Mastery: Beginner → Intermediate → Advanced

Welcome to the **SQL Concepts Repository**! 🎉

This repo is designed to take you on a full journey through SQL — starting from the basics, moving through intermediate concepts, and ending with advanced topics used in real-world production systems.

Whether you're a student, a backend dev, a data analyst, or someone leveling up for interviews, this repo has everything you need.

---

## 📚 What You’ll Learn

This repository is divided into **three main levels**, each building on the previous one.

---

# 1️⃣ Beginner Level (Foundations)

Perfect for total beginners or anyone who wants a strong base.

### ✔ Basic Concepts

* What is SQL?
* Types of SQL (DDL, DML, DCL, TCL)
* Database vs Table vs Column vs Row
* Primary Keys & Foreign Keys

### ✔ Basic SELECT Queries

* `SELECT` basics
* Filtering with `WHERE`
* Sorting with `ORDER BY`
* Limiting rows with `TOP` or `LIMIT`

### ✔ Data Manipulation

* `INSERT`
* `UPDATE`
* `DELETE`

### ✔ Table Creation (DDL)

* `CREATE TABLE`
* Data types (INT, VARCHAR, DATE, FLOAT, etc.)
* `NOT NULL`, `DEFAULT`, `UNIQUE`

---

# 2️⃣ Intermediate Level (Strengthening Your Skills)

For learners who know the basics and want to work like a real dev.

### ✔ Joins

* `INNER JOIN`
* `LEFT JOIN`
* `RIGHT JOIN`
* `FULL OUTER JOIN`
* Joining multiple tables

### ✔ Aggregations

* `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`
* `GROUP BY`
* `HAVING`

### ✔ Subqueries

* Subqueries in `SELECT`
* Subqueries in `WHERE`
* Subqueries in `FROM` (derived tables)

### ✔ Constraints

* Primary Key
* Foreign Key
* Unique Key
* Check Constraints

### ✔ Indexes

* Clustered index
* Non-clustered index
* When to use indexes
* Performance considerations

---

# 3️⃣ Advanced Level (Real-World SQL)

Things used in production systems, performance tuning, and data engineering.

### ✔ Window Functions (Analytical Functions)

* `ROW_NUMBER()`
* `RANK()` / `DENSE_RANK()`
* `OVER (PARTITION BY ...)`
* Running totals & moving averages

### ✔ Advanced Joins

* Self-joins
* Cross joins
* Many-to-many relationships

### ✔ CTEs (Common Table Expressions)

* Basic CTE
* Recursive CTE

### ✔ Transactions & Locking

* `BEGIN`, `COMMIT`, `ROLLBACK`
* Isolation levels
* Deadlocks

### ✔ Performance Optimization

* Query execution plans
* Index tuning
* Avoiding full table scans
* Normalization & denormalization

### ✔ Stored Procedures, Functions & Triggers

* Creating stored procedures
* Creating user-defined functions
* Creating triggers for automation

---

# 📁 Repository Structure

```
/sql-mastery-repo
│
├── beginner/
│   ├── select-basics.sql
│   ├── inserts-updates-deletes.sql
│   ├── create-tables.sql
│   └── constraints.sql
│
├── intermediate/
│   ├── joins.sql
│   ├── group-by.sql
│   ├── subqueries.sql
│   ├── indexes.sql
│   └── views.sql
│
├── advanced/
│   ├── window-functions.sql
│   ├── performance-tuning.sql
│   ├── transactions.sql
│   ├── ctes.sql
│   └── stored-procedures.sql
│
└── README.md
```

---

# 🎯 Goal of This Repository

To give you a **complete SQL learning path** with:

* Clean, explained examples
* Real-world scenarios
* Optimized best practices
* Tasks & challenges at each level

By the end, you’ll confidently write efficient SQL queries used by professionals.

---

# 🚀 Who This Repo Is For

* Beginners learning SQL from scratch
* Devs preparing for interviews
* Backend engineers & data analysts
* Students working on database courses
* Anyone wanting to deeply understand SQL

---

# 🤝 Contributing

Contributions are welcome!
If you want to improve examples, add new topics, or fix mistakes, feel free to create a pull request.

---

# ⭐ Support

If this repo helps you, give it a **star** ⭐ — it motivates future content and improvements.

---

If you want an additional **PDF version**, **GitHub badges**, or even a **logo design** for the repo, just ask!
